# Readl Electron App

Paste text, choose voice/lang/speed, and play audio from the local Kokoro TTS service.

## Run

```bash
cd electron-app
npm install
npm start
```

Ensure the Python service is running at `http://127.0.0.1:8000`.
